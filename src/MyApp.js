import React, { Component } from 'react'
import './style.css'
import products from './productsData'



function MyApp() {

  let names = [];

  return (products.forEach((product, key) => {
    return (product.name)
  }))
  
}

export default MyApp